"""bjoin studio additions to LOGIK-PROJEKT.

Deliberately a SELF-CONTAINED package with a single call site in
`projekt_creator.py`, so that when we merge upstream's 2026.2.1 refactor
(239 files changed) our studio logic is one directory to re-drop and one
line to re-add, rather than edits scattered through their tree.
"""
